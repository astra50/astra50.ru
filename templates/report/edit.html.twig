{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Редактирование отчёта № {{ entity.id }}</h1>
    <hr>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Сохранить</button>
    {{ form_end(form) }}
{% endblock %}
